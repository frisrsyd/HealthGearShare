@layout('layouts/main')
@set('title', 'Detail Barang')

@section('body')
<section>
    <div class="container">
        <div class="row">
            @search()
            @end
        </div>
        <div class="row">
            {{-- if errror = true set subtitle barang tidak --}}
            @if(error)
            @subtitle({
            subtitle: error,
            })
            @end
            @end

            {{-- if errror = false set subtitle barang tersedia --}}
            @if(!error)
            @subtitle({
            subtitle: 'Barang yang tersedia',
            })
            @end
            @end
        </div>
        <div class="row">
            @each(item in tools)
            @card.cardMediumThree({
            nama_barang: item.name,
            label_1: 'Penyedia:',
            label_2: 'Jumlah Tersedia:',
            label_3: 'Link Google Maps:',
            text_1: item.user.name,
            text_2: item.available,
            text_3: 'Klik Disini',
            target_3: '_blank',
            href_text_3: item.maps,
            text_button: 'Pinjam',
            href: '/data-peminjaman/' + item.slug,
            image : 'http://localhost:8080/assets/images' + item.image.url,
            })
            @end
            @endeach
        </div>

    </div>
</section>
@endsection